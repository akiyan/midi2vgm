# PSG mapping directory

`psg.txt` is a historical example map for assigning MIDI channels to the SN76489
tone/noise channels for PSG-only conversion.

Generated `*_PSG.vgm` files are ignored by git.
