# PSG mapping directory

`psg.txt` is read by `scripts/genallpsg.py` and maps MIDI channels to the
SN76489 tone/noise channels for PSG-only conversion.

Generated `*_PSG.vgm` files are ignored by git.
